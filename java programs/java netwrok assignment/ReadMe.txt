The client class files are in MainClient
The server class files are in MainServer.

To run Server:
	cd MainServer
	java main.MainServer

To run Client 
	cd MainClient
	java main.MainClient [ serverIP ]
	
	Note : if serverIP is not provided on commandline, it is assumed to be localhost.
